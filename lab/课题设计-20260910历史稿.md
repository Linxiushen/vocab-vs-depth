# 2026-09-10 历史稿：包含已撤回的结论

> 请以同目录《课题设计.md》的 2026-09-14 修订版为准。本稿保留实验历史，
> 其中预算、有效 token 数、验证集独立性及若干因果判断已被更正。

# 课题：固定端侧预算下，中文小模型的参数该花在词表还是深度？

> 2026-09-10 立项 · 陈旭林 · 全部数字为本机实测，非引用

## 一句话

在总参数量、部署体积都锁死在 **64M / ~68MB (Q8_0)** 的前提下，把约 7.4M 参数
花在「更大的词表」和花在「多一层 Transformer」，哪个得到的中文模型更好？

## 为什么这是个真问题

MiniMind 的 README 明确写「不建议重新训练 tokenizer」，但给出的理由是
**生态兼容性**（换词表会导致社区模型不可复用），**不是效果**。

而端侧部署恰恰不在乎生态兼容 —— 模型只服务自己的设备。所以在端侧场景下
「6400 词表是不是最优」是一个**被官方回避、且没人验证过**的问题。

它值得做，是因为两个方向的效应相反、必须实测才知道净值：

| | 换大词表的影响 |
|---|---|
| 参数 | ❌ embedding 变大，挤占 Transformer 层的预算 |
| 算力 | ✅ 中文 token 数减少 → 同样算力能多看文本 |
| 上下文 | ✅ 同样 `max_seq_len` 能装下更多汉字 |
| 端侧解码 | ❓ lm_head 矩阵变大（decode 每步都要算），但序列变短 |

## 本机实测的立项依据

以下全部是在 MacBook Air M5 / 24GB 上跑出来的，不是引用：

### 1. 6400 词表对中文的压缩比很差

| tokenizer | vocab | 中文压缩比（加权平均） |
|---|---|---|
| MiniMind | 6,400 | **1.19 字符/token** |
| MiniCPM5-1B | 130,560 | **1.69 字符/token** |

同一批中文语料，MiniMind 词表要多切出 **43%** 的 token。
实例：`往左转三十度然后坐下` → `['往','左','转','三','十','度','然后','坐','下']`
—— 几乎是字级切分。

### 2. 一层的参数成本 ≈ 词表扩容的参数成本

```
加 1 层 (d=768):        +7.37M
词表 6400 → 16384:      +7.67M      ← 几乎相等，这是实验能干净对齐的原因
```

### 3. 实验臂（参数量严格对齐）

| 臂 | vocab | L | d | ffn | 总参数 | emb 占比 |
|---|---|---|---|---|---|---|
| **A** 官方基线 | 6,400 | 8 | 768 | 2432 | 63.91M | 7.7% |
| **B′** 换词表砍层 | 16,384 | 7 | 768 | 2432 | 64.21M | 19.6% |

偏差 +0.30M（0.5%），可接受。两臂 Q8_0 体积都在 68MB 附近。

### 4. 算力预算

| | 实测/官方 |
|---|---|
| M5 纯算力（bf16, 64M） | **1.76 TFLOPS**，4,577 tok/s |
| M5 真实训练吞吐（30M, fp32, seq512） | **6,827 tok/s**（60 秒定点采样），与纯算力基准 6,676 一致 |
| M5 内存峰值（64M） | 0.43 GB —— 内存完全不是瓶颈，**不要动 `iogpu.wired_limit_mb`** |
| 3090 官方锚点 | 64M 全流程 (pretrain+SFT) **2.31h / ¥3.0** |
| M5 : 3090 | 约 **1 : 8~11** |

**结论：Mac 用来理解和冒烟，租卡用来出正式结果。** 两臂正式训练合计约 ¥3.2。

---

## 顺手发现的第二个问题（可作为子课题）

`PretrainDataset` 把每条样本 pad 到 `max_length`，而 `pretrain_t2t_mini`
的平均长度只有 **208 tokens**：

| max_seq_len | 每 epoch 前向 token | 真实内容 | 浪费 |
|---|---|---|---|
| 512 | 650M | 264M | **59%** |
| 768 | 976M | 264M | **73%** |

labels 里 pad 位置是 -100，所以 **loss 是对的**，但前向和反向**照样把所有位置
都算了一遍**。这是纯粹白烧的算力，正是 sequence packing 要解决的问题。

这个发现顺带解释了官方耗时表的一个矛盾：README 说 `pretrain_t2t_mini`
推荐 `max_seq_len≈768`，但脚本默认是 **340**。按 976M token 反推 3090 需要
86 TFLOPS 有效算力（不可能）；按 340 的 432M token 反推是 38 TFLOPS（合理）。
**所以官方 1.21h 那个数字是按默认 340 跑的，不是按 README 推荐的 768。**

子课题：实现 sequence packing，验证能否在不掉点的前提下把训练时间压到 1/3。

## 第三个发现：不要相信日志里的 ETA（一次自己打自己脸的记录）

冒烟训练日志里的 `epoch_time`（ETA）在 step 1000→1180 之间从 65min 一路涨到
206min，看起来像是严重降速。我据此得出「数据管线吃掉 35% 算力」的结论。

**这个结论是错的。** 用 60 秒定点采样实测：

```
step 1220 → 1320，60 秒 100 步 = 6,827 tok/s
```

而 `bench_m5.py` 的纯算力基准是 6,676 tok/s。**真实训练吞吐 ≈ 纯算力基准，
数据管线并没有成为瓶颈。**

ETA 会爆炸是因为源码里它是个**累计平均**：

```python
eta_min = spend_time / max(step - start_step, 1) * (iters - step) // 60
```

只要早期有一段慢速，后面就一直被拖累，即使当前速度已经恢复。而那段早期慢速
很可能是我自己造成的 —— 我在 step 500–1000 期间跑了 unigram 熵统计
（tokenize 2 万篇文档，吃满 CPU），和训练抢了资源。

**两条教训：**
1. **测吞吐要定点采样，不要读累计 ETA。** 累计平均会把任何一次瞬时抖动
   永久地烙进后面所有的读数。
2. **在无风扇的 MacBook Air 上跑训练时，不要同时跑别的重活。** 我在文档里
   写了这条警告，然后自己撞了上去。

（顺带：本次排查确认热压力正常 —— `pmset -g therm` 无任何 thermal warning，
训练进程 RSS 仅 650MB，所以降速不是热降频也不是内存压力。）

---

## 第四个发现：评测方法能让同一个模型的分数差 50%

冒烟模型训完后，第一次评测得到 val loss **6.51**，而训练末段 loss 只有 ~4.0。
差 2.5 nats，看起来像灾难性过拟合 —— 1 epoch、30M 模型、4 万条数据，
这不可能。所以问题在评测，不在模型。

查出两个独立的 bug，**修完之后模型权重一个字节都没动，BPB 降了 50%**：

### Bug 1（大头，−41%）：验证集切法

`pretrain_t2t_mini.jsonl` 是**按来源排序的，完全没有 shuffle**：

| 位置 | 内容 | 中位长度 |
|---|---|---|
| 头部 3000 条 | 中文指令/诗歌对话 | 231 字符 |
| 尾部 3000 条 | **英文考试选择题** | **1,251 字符** |

「从尾部切 2000 行做验证集」——这是很多教程（包括我最初写的
`split_val.py`）的默认做法——切出来的是一个**全英文、长度是训练数据 5 倍**
的集合。模型从没见过那个分布。

改成按行号哈希做确定性随机划分（`seed=42`，可复现，单遍扫描内存恒定）后，
验证集变成中位 304 字符、93% 含中文，与全语料同分布。

### Bug 2（−5.7%）：train/serve 不一致

`PretrainDataset` 喂给模型的每条是 `[bos] + tokens + [eos]`，
而我的评测脚本喂的是裸 token 序列。模型训练时永远先看到 `<|im_start|>`，
评测时却没有 —— 前缀分布不同，第一个 token 还要从零上下文预测。

### 完整归因表（同一份权重，只改评测）

| 评测配置 | val loss | BPB |
|---|---|---|
| 尾部验证集 + 无 bos ← 最初 | 6.511 | 3.307 |
| 全局随机验证集 + 无 bos | 4.731 | 1.953 |
| 全局随机验证集 + 有 bos | 4.428 | 1.841 |
| **同分布对照集 + 有 bos** | **4.100** | **1.656** |

训练末段 loss ≈ 4.0–4.2，同分布 val loss = 4.100 —— **几乎重合，
这才是 1 epoch 该有的样子。**

### 教训

> **在相信任何「模型不好」的结论之前，先证明你的评测是对的。**
> 一个错的评测集能让健康的训练看起来像灾难，而且它不会报错。

这也是为什么课题里所有对比都必须：① 验证集随机切且固定 seed；
② 评测输入格式与训练完全一致；③ 报 BPB 而不只是 loss。

---

## 方法学：三条必须遵守的规则

### 规则 1 — 跨 tokenizer 比较必须用 BPB，不能用 loss/PPL

loss 是「每 token 的平均负对数似然」。词表越大 → token 越少 → 每个 token
承载的信息越多 → loss 天然越高。**A 和 B′ 的 loss 直接比是无效的。**

```
BPB = 总NLL(nats) / (ln2 × 总UTF-8字节数)
```

字节是与 tokenizer 无关的物理量，所以 BPB 可比。工具：`lab/eval_bpb.py`

### 规则 2 — 必须有验证集，且必须从训练集物理删除

`train_pretrain.py` 只有 `train_ds`，没有验证集 —— 这是仓库的第一个洞。
没有验证集无法区分「在学习」和「在背题」。工具：`lab/split_val.py`

### 规则 3 — 公平性有两种口径，都要报

| 口径 | 含义 | 回答的问题 |
|---|---|---|
| **同语料** | 两臂看同一批文本 | 「我该发布哪个模型？」（B′ 还更省算力） |
| **同算力** | 两臂用同样的 FLOPs | 「词表本身值不值？」（A 看的文本更少） |

只报一个口径就是在挑对自己有利的说法。

---

## 五条 loss 判据（训练时实时对照）

### 两条锚线（本机实测，不是拍脑袋的阈值）

在 `pretrain_t2t_mini` 上用 MiniMind 6400 词表实测：

| 锚线 | loss (nats) | BPB | 含义 |
|---|---|---|---|
| **H₀ = ln(V)** | **8.764** | 3.541 | 随机初始化的期望 loss |
| **H₁ = unigram 熵** | **7.218** | 2.917 | 只学会字频、还不懂上下文 |
| 冒烟模型 (30M, 20.5M token) | 4.100 | **1.656** | 实测，同分布验证集 |

（BPB 换算用实测的 **3.57 字节/token**。我最初按 2.53 估算，是错的 ——
那个数字来自手挑的纯中文样本，而真实语料含英文和标点。**换算系数必须实测。**）

换词表后必须重算（16384 词表的 H₀ = 9.704，H₁ 需重新统计）。

| # | 判据 | 失败意味着 |
|---|---|---|
| 1 | step-0 loss ≈ **H₀** | 不在 ±0.2 内 = labels/padding mask 错了，或权重初始化被污染 |
| 2 | **看过约 2M token 内跌破 H₁** | 跌破 H₁ 才说明模型开始用上下文，而不只是背字频。**这是真正的早期里程碑** |
| 3 | loss 对 log(token) 近似线性 | 出现平台期 = 数据重复度高或 lr 衰减太早 |
| 4 | 无 NaN / 尖峰 | fp16 无 GradScaler → 下溢；grad_clip 被关；lr 对 batch 太大 |
| 5 | SFT loss 收敛到 1.0–2.0 | 跌破 0.3 = 背题，减 epoch |

### ⚠ 判据 2 必须按 token 数写，不能按 step 数写

我最初把判据 2 写成「500 步内跌破 5.0」，**这是错的**，会导致误杀健康的训练。

原因：`step` 在 MiniMind 的日志里计的是 **micro-batch**，一步看到的
token = `batch_size × max_seq_len`，所以换 batch size 后同样的步数对应完全
不同的数据量。实测对比：

| 配置 | tokens/step | step 500 时累计 |
|---|---|---|
| 官方默认 (b32, seq340) | 10,880 | 5.44M |
| 本次冒烟 (b8, seq512) | 4,096 | **2.05M** |

同样是 step 500，看到的数据量差 2.7 倍。**任何按 step 写的阈值都不可移植。**

本次冒烟的实际结果：step 500 / 2.05M token，loss = 6.63，**已跌破 H₁ (7.218)
0.59 nats** —— 训练健康。若按错误的「500 步 < 5.0」判据，会误杀这次运行。

---

## 阶段计划

| 阶段 | 内容 | 在哪 | 耗时 | 花费 |
|---|---|---|---|---|
| **0** | 环境 + 跑官方成品祛魅 | Mac | 1h | ¥0 |
| **1** | 冒烟训练 30M，验证五条判据，逐行读源码 | Mac | 3h | ¥0 |
| **2** | 补验证集 + BPB 评测；实测 padding 浪费 | Mac | 2h | ¥0 |
| **3** | 训 16384 词表（含 GPT-2 正则修复） | Mac | 1h | ¥0 |
| **4** | 租卡跑臂 A + 臂 B′，两种口径各一次 | 3090 | 5h | **¥7** |
| **5** | BPB/CEval 对比 + 量化导出 + 端侧实测 | Mac+板子 | 4h | ¥0 |
| **6** | 写实验报告 | — | 3h | ¥0 |

**总计约 19 小时、¥7。**

---

## 预期结论（先写下来，避免事后编故事）

我的先验：**B′ 会赢，但赢得不多（BPB 差距 < 3%），且主要赢在算力效率而非质量。**

理由：64M 这个量级下，中文知识能力必然在随机线附近（C-Eval 24-26），
所以两臂在下游任务上大概率无法区分（README 自报 ceval stderr ±1.2，
两模型比较的差值标准误约 ±1.7，**小于 3 个百分点的差异不足以区分**）。
真正能区分的只有 BPB 和训练吞吐。

**如果实测推翻这个先验，那才是这个课题真正有价值的地方 —— 记得把先验留在报告里。**

## 明确不做的事

- ❌ 不追求「训出一个好用的模型」。64M 的中文知识能力必然在随机线，这是
  token 预算问题不是努力问题。参照：Steel-LLM 一个人 8 个月 / 8×A100 /
  1.1T tokens 训出 1B，CEval 仍在随机线附近。
- ❌ 不把这个课题挂到 MicroCat 的产品路线上。产品要的是 MiniCPM5-1B + LoRA。
- ❌ 不碰 RL/GRPO。Mac 上 GRPO 是 0.02 it/s（50 秒一步），等于不可用。

---

## 工具清单

| 文件 | 作用 |
|---|---|
| `lab/bench_m5.py` | 实测本机训练吞吐与有效 TFLOPS，校准所有工期估算 |
| `lab/split_val.py` | 切验证集并从训练集物理删除（补仓库的洞） |
| `lab/eval_bpb.py` | BPB 评测，跨 tokenizer 唯一可比的指标 |

## 环境（已装好）

```
Python 3.12.14 (uv venv at ./.venv)
torch 2.14.0          MPS 可用
transformers 4.57.6   ← 必须钉住，MiniMind 模型代码不兼容 5.x
datasets 3.6.0        ← 必须钉住
numpy 1.26.4
```

数据：`pretrain_t2t_mini.jsonl` 1.2GB / 1,270,238 条 ·
`sft_t2t_mini.jsonl` 1.6GB / 905,718 条

## Mac 上跑训练的四条硬约束（读源码确认）

1. `--num_workers 0` 必须加 —— macOS 的 fork 语义 + tokenizers 并行会挂
2. `--dtype bfloat16` 在 MPS 上**无效**：源码
   `device_type = "cuda" if "cuda" in args.device else "cpu"`，非 cuda 时
   `autocast_ctx = nullcontext()`，实际是 fp32 训练。想要 bf16 只能直接
   `model.to(dtype=torch.bfloat16)`（实测快 20%、省一半内存）
3. `--epochs` 默认是 **2** 不是 1
4. MacBook Air 无风扇，连续满载会热降频。用 `caffeinate -i`，别拿前 50 步外推
